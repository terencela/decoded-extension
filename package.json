{
  "name": "@workspace/decoded-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/generate-icons.mjs",
    "build": "vite build",
    "dev": "node scripts/generate-icons.mjs && vite build --watch"
  },
  "dependencies": {
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.290",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "vite": "catalog:",
    "vite-plugin-web-extension": "^4.1.1"
  }
}
